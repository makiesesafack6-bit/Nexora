/* Nexora OpenAI bridge: the API key stays server-side. */
(function(){
  const getJSON=(key,fallback={})=>{try{return JSON.parse(localStorage.getItem(key)||JSON.stringify(fallback))}catch{return fallback}};
  const save=(key,value)=>localStorage.setItem(key,JSON.stringify(value));
  const parseScore=v=>{const n=parseFloat(String(v??'').replace('%',''));return Number.isFinite(n)&&n>=0&&n<=100?Math.round(n):null};
  const demoScore=(p,context='')=>{const raw=String(p?.id||p?.handle||p?.name||'prospect')+'|'+String(context||'');let h=0;for(let i=0;i<raw.length;i++)h=(h*31+raw.charCodeAt(i))>>>0;return 55+(h%41)};
  const scoreFor=(p,context='')=>parseScore(p?.matchScore??p?.score??p?.match) ?? demoScore(p,context);
  const withScore=(p,score,context='')=>{const s=parseScore(score) ?? scoreFor(p,context);return {...p,matchScore:s,score:s,match:`${s}%`};};
  async function rank(profile,search,prospects){const res=await fetch('/api/openai-match',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({profile,search,prospects})});if(!res.ok)throw new Error('AI matching unavailable');const data=await res.json();return Array.isArray(data.matches)?data.matches:[];}
  window.NexoraAI={rank,scoreFor,withScore};

  function profileCandidates(){const engine=window.NexoraMatching;if(!engine?.catalog?.length)return [];const category=engine.categoryForProfile?.();let candidates=engine.catalog.slice();if(category)candidates=candidates.filter(p=>p.category===category);return candidates.map(p=>withScore(p,null,''));}
  function installPreparedMatcher(list){if(!Array.isArray(list)||!list.length||!window.NexoraMatching)return;const base=window.NexoraMatching;if(!base.__originalMatches)base.__originalMatches=base.matches.bind(base);const original=base.__originalMatches;base.matches=function(query=''){const q=String(query||'').trim();const source=list.map(p=>withScore(p,p.matchScore,q));if(!q)return source;const terms=q.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').split(/[^a-z0-9]+/).filter(x=>x.length>2);return source.filter(p=>terms.some(t=>[p.need,p.name,p.role,p.skills,p.sector,p.keywords].flat().join(' ').toLowerCase().includes(t)));};base.originalMatches=original;}

  async function prepareQuizMatches(){const profile=getJSON('nexoraProfile',{}),prospects=profileCandidates();if(!Object.keys(profile).length||!prospects.length)return;try{const ai=await rank(profile,'',prospects),byId=new Map(prospects.map(p=>[String(p.id||p.handle||p.name),p])),prepared=ai.map(m=>{const base=byId.get(String(m.id));return base?withScore({...base,aiReason:m.reason||'',need:m.displayNeed||base.need},m.score,''):null}).filter(Boolean);if(prepared.length){save('nexoraPreparedMatches',prepared);installPreparedMatcher(prepared)}localStorage.setItem('nexoraAIMatchedAt',new Date().toISOString());}catch{const prepared=prospects.map(p=>withScore(p,p.matchScore,''));save('nexoraPreparedMatches',prepared);installPreparedMatcher(prepared);}}
  const watch=setInterval(()=>{const card=document.getElementById('analysisCard');if(card&&!card.classList.contains('hidden')){clearInterval(watch);prepareQuizMatches();}},250);
  const stored=getJSON('nexoraPreparedMatches',[]);if(stored.length)installPreparedMatcher(stored);

  function setLoading(overlay,pct,stage){if(!overlay)return;overlay.classList.remove('hidden');let box=overlay.querySelector('.nx-ai-progress');if(!box){box=document.createElement('div');box.className='nx-ai-progress';box.style.cssText='width:min(520px,88vw);margin-top:18px';box.innerHTML='<div style="display:flex;justify-content:space-between;font:700 14px DM Sans"><span id="nxAiStage">Analyse de votre demande…</span><b id="nxAiPct">0%</b></div><div style="height:8px;background:#e7eaf0;border-radius:99px;margin:10px 0 16px;overflow:hidden"><i id="nxAiBar" style="display:block;width:0;height:100%;background:#111827;border-radius:99px"></i></div><div style="display:grid;gap:7px;text-align:left;font:600 13px DM Sans;color:#687287"><span>✓ Profil Nexora pris en compte</span><span>✓ Demande Find envoyée à l’IA</span><span>✓ Prospects compatibles comparés</span><span>✓ Scores calculés selon la pertinence</span></div>';overlay.appendChild(box)}const bar=box.querySelector('#nxAiBar'),p=box.querySelector('#nxAiPct'),s=box.querySelector('#nxAiStage');if(bar)bar.style.width=pct+'%';if(p)p.textContent=pct+'%';if(s)s.textContent=stage;}

  async function runFind(){const input=document.getElementById('findInput'),query=input?.value?.trim()||'',profile=getJSON('nexoraProfile',{});if(!query){input?.focus();return}const overlay=document.getElementById('searchLoading'),candidates=profileCandidates();if(!candidates.length)return;let ai=[];const started=Date.now();setLoading(overlay,2,'Analyse de votre demande…');const progress=setInterval(()=>{const e=Date.now()-started,p=Math.min(94,Math.floor(e/15000*94));let stage='Analyse de votre demande…';if(e>=3500&&e<7000)stage='Recherche des opportunités compatibles…';else if(e>=7000&&e<10500)stage='Comparaison des profils…';else if(e>=10500)stage='Classement des meilleurs matchs…';setLoading(overlay,p,stage)},200);try{ai=await rank(profile,query,candidates)}catch(e){console.warn('Nexora AI Find unavailable',e)}const remaining=Math.max(0,15000-(Date.now()-started));setTimeout(()=>{clearInterval(progress);setLoading(overlay,100,'Recherche terminée — résultats prêts.');const byId=new Map(candidates.map(p=>[String(p.id||p.handle||p.name),p]));const prepared=(ai||[]).map(m=>{const base=byId.get(String(m.id));return base?withScore({...base,aiReason:m.reason||'',need:m.displayNeed||base.need},m.score,query):null}).filter(Boolean);const final=(prepared.length?prepared:candidates).map(p=>withScore(p,p.matchScore,query));save('nexoraFindMatches',final);setTimeout(()=>{renderFindResults(final);overlay?.classList.add('hidden')},700);},remaining);}

  function renderFindResults(list){const table=document.querySelector('.prospect-table');if(!table)return;table.querySelectorAll('.prospect.find-result').forEach(x=>x.remove());list.slice(0,5).forEach((p,i)=>{const item=withScore(p,p.matchScore,p.search||'');const row=document.createElement('div');row.className='prospect find-result nexora-ai-result';row._nexoraProspect=item;row.dataset.matchScore=String(item.matchScore);row.style.cssText='cursor:pointer;animation:nxFindIn .45s ease '+(i*.1)+'s both';row.innerHTML='<div class="person"><div class="person-avatar a1">'+(item.initial||item.name?.[0]||'N')+'</div><div><strong>'+item.handle+'</strong><small>'+item.name+'</small></div></div><span class="source">'+item.source+'</span><p>'+item.need+'</p><b class="match high">'+item.match+'</b><button type="button" class="find-detail">Voir les détails →</button>';const open=()=>window.NexoraOpenProspect?.(i,item);row.onclick=e=>{if(!e.target.closest('button'))open()};row.querySelector('.find-detail').onclick=e=>{e.stopPropagation();open()};table.appendChild(row)});document.getElementById('prospectEmpty')?.remove();const count=document.getElementById('prospectsCount');if(count)count.textContent=String(Math.min(list.length,5));document.querySelector('.nav-item[href="#prospects"]')?.click();}
  document.addEventListener('click',e=>{const btn=e.target.closest?.('#findButton,#quickFind');if(!btn)return;e.preventDefault();e.stopImmediatePropagation();runFind()},true);
  window.NexoraPrepareQuizMatches=prepareQuizMatches;
})();
